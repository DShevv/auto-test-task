# Auto Test Task - Интернет-магазин

Тестовое задание - веб-приложение интернет-магазина с каталогом товаров и корзиной.

## 🚀 Запуск проекта

### Требования

- Node.js 18+
- npm или yarn

### Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

### Тестирование

```bash
# Запуск тестов
npm run test

# Запуск тестов с UI
npm run test:ui

# Запуск тестов с покрытием
npm run test:coverage
```

### Линтинг

```bash
npm run lint
```

## ⚙️ Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# URL API для получения товаров (опционально)
# По умолчанию используется https://fakestoreapi.com
VITE_API_URL=https://fakestoreapi.com
```

## 🛠️ Технологический стек

### Стейт-менеджмент и работа с данными

| Технология         | Назначение                                                                  |
| ------------------ | --------------------------------------------------------------------------- |
| **Redux Toolkit**  | Управление глобальным состоянием (корзина, тема)                            |
| **TanStack Query** | Кэширование и управление серверным состоянием (загрузка товаров, категорий) |

### Стилизация

| Технология       | Назначение                                                  |
| ---------------- | ----------------------------------------------------------- |
| **Tailwind CSS** | Utility-first CSS фреймворк используется библиотекой Sonner |
| **SCSS Modules** | Модульные стили для компонентов                             |

### Роутинг и UI

| Технология       | Назначение         |
| ---------------- | ------------------ |
| **React Router** | Клиентский роутинг |
| **Sonner**       | Toast-уведомления  |

### Тестирование

| Технология          | Назначение                     |
| ------------------- | ------------------------------ |
| **Vitest**          | Unit-тестирование              |
| **Testing Library** | Тестирование React компонентов |
| **jsdom**           | Эмуляция DOM для тестов        |

## 📁 Структура проекта

```
src/
├── api/                    # API сервисы и типы
│   ├── categoryService.ts  # Сервис категорий
│   ├── productService.ts   # Сервис товаров
│   ├── hooks.ts            # React Query хуки
│   └── types.ts            # TypeScript типы API
├── assets/                 # Статические ресурсы
│   └── icons/              # SVG иконки как React компоненты
├── components/
│   ├── blocks/             # Крупные блоки (Header, Layout, Filters)
│   ├── cart/               # Компоненты корзины
│   ├── product/            # Компоненты товаров
│   └── ui/                 # UI компоненты (Button, Select, Skeleton)
├── conf/                   # Конфигурация
├── hooks/                  # Кастомные хуки
├── pages/                  # Страницы приложения
│   ├── CartPage/           # Страница корзины
│   └── CatalogPage/        # Страница каталога
├── store/                  # Redux store
│   └── slices/             # Redux слайсы (cart, theme)
└── utils/                  # Утилитарные функции
```

## 🎯 Обоснование выбора технологий

### Redux Toolkit + TanStack Query

- **Redux Toolkit** - для клиентского состояния (корзина, тема). Простой API, встроенный Immer, DevTools.
- **TanStack Query** - для серверного состояния. Автоматическое кэширование, рефетчинг, обработка loading/error состояний.

### SCSS Modules

Модульные стили для компонентов с изолированной областью видимости, что предотвращает конфликты имён классов.

### Vitest

Нативная интеграция с Vite, быстрое выполнение тестов, совместимость с Jest API.
